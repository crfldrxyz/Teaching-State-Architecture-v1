[project]
name = "teaching-state-architecture"
version = "0.2.0"
description = "Deterministic, state-driven teaching integrity engine"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"
